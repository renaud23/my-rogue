function versus(attacker, defender, weapeon) {
  console.log({ attacker, defender, weapeon });
  // compute AR & DR
  // résolve fight
  // remove life
  return [attacker, defender];
}

export default versus;
